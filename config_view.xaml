<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Element Selector" Height="500" Width="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Config Element Selector" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="10"/>

        <!-- Search Box -->
        <StackPanel Grid.Row="1" Margin="10">
            <TextBlock Text="Search:" FontSize="14" FontWeight="Bold" Margin="0,0,0,5"/>
        </StackPanel>

        <!-- ListBox with CheckBoxes -->
        <StackPanel Grid.Row="2" Margin="10">
            <TextBlock Text="Select Elements:" FontSize="14" FontWeight="Bold" Margin="0,0,0,5"/>
        </StackPanel>

        <!-- RadioButtons for Check All/None -->

        <!-- OK and Cancel Buttons -->
    </Grid>
</Window>
